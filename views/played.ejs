<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cron</title>
</head>
<body>
    
    <% recentlyPlayed.forEach(content => { %>
        <div>
            <img src="<%= content.track.album.images %>" alt="">
            <%= content.track.name %>
        </div>
    <% }) %>

    

</body>
</html>